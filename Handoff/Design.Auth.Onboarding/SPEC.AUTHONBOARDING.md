# SPEC — 인증 · 온보딩 (전 화면 공통: 데스크톱 기준, 배경 `#f4f6fa`, 카드 중앙 정렬)

공통 스타일: 카드 = 흰 배경 + 1.5px `#e6e8ee` 보더 + radius 18~20px + shadow `0 8px 32px rgba(28,43,74,.05~.06)`.
입력 필드 = 높이 48px, 1.5px `#e6e8ee` 보더, radius 11px, 포커스 시 2px `#23408e` 아웃라인.
주 버튼 = 오렌지 `#FF6B35`(hover `#e85a26`), 높이 50px, radius 11px, shadow `0 4px 14px rgba(255,107,53,.3)`.
보조 버튼 = 흰 배경 + 보더(hover `#f4f6fa`). 진행바 세그먼트 = 높이 5px, radius 3px, 완료 오렌지/미완 `#e6e8ee`.
**카피는 레퍼런스 HTML의 원문 그대로. 임의 수정 금지.**

---

## ① 로그인 (`/login`) — 카드 400px, padding 40×36
1. 타이틀 "시작하기"(24px/800) + 서브 "가입과 로그인이 한 번에 — 계정이 없으면 자동으로 만들어 드려요."(14px, 2줄 중앙)
2. 소셜 버튼 3개 (높이 50px, radius 11, 15px/700, 아이콘 19px + gap 10):
   - Google (흰 배경+보더) / Kakao (#FEE500, 텍스트 #191919) / LINE (#06C755, 흰 텍스트)
   - "~로 계속하기" 표기 통일
3. 준비중 행: 네이버 · Apple — 높이 44px, 점선 보더, 텍스트 `#b6bdc9` "(준비중)", 클릭 불가
4. 구분선 "또는 이메일로" (양쪽 1px `#e6e8ee` 라인, 12.5px `#b6bdc9`)
5. 이메일 · 비밀번호 입력 + "이메일로 계속하기" 버튼 (네이비 `#23408e`, hover `#1c2b4a`)
6. 법적 고지 12px `#8a93a6` 중앙: 약관·개인정보 링크(밑줄) + "로그인 시 General 역할로 자동 가입됩니다."

동작: 모든 로그인 성공 → `/settings/select-role`. (기존 유저이고 역할 보유 시 → 대시보드 직행)

## ② 역할 선택 (`/settings/select-role`) — 폭 560px
- 타이틀 "어떤 역할로 활동하시나요?"(26px/800) + 서브 "역할에 맞는 대시보드와 온보딩을 준비해 드려요. 나중에 설정에서 바꿀 수 있어요."
- 역할 카드 3개 (가로형: 아이콘 52px 박스 + 텍스트 + →, padding 22×24, radius 16, hover: 네이비 보더 + 그림자):
  1. ⚽ **선수 · 학부모** — "선수 이력·경기기록 관리 · 학부모는 자녀 프로필을 대리 관리" → `/onboarding/player`
  2. 🏛️ **팀 관리자 · 코치** (아이콘 박스 네이비) — "팀 홈페이지 자동 생성 · 선수단·일정·경기 결과 운영" → `/onboarding/team`
  3. 🤝 **에이전트** — **비활성** (점선 보더, opacity .7, 클릭 무시): "준비중" 뱃지(Agent Violet `#6d4bd8` 10% 배경) + "선수 스카우팅 · 팀 추천 — 별도 서비스로 오픈 예정"
- 하단 텍스트 링크 "나중에 선택할게요 (둘러보기)" → General 상태로 대시보드

## ③a 선수 온보딩 (`/onboarding/player`) — 폭 480px, 3스텝
헤더: 스텝 타이틀(23px/800) + "n / 3" + 진행바 3세그먼트.
- **1. 선수 기본 정보**: 이름("예: 김민준") · 생년월일("YYYY.MM.DD") + 안내 박스(teal 톤 `#f4f8f7`): 👪 "학부모이신가요? 자녀의 정보를 입력하면 **보호자 대리 관리** 프로필로 만들어져요…"
- **2. 연령대와 종목**: 연령 칩 U12/U15/U18 (단일 선택, 선택=네이비 채움) · 종목 = ⚽ 축구 고정(비활성 필드 + "현재 축구만 지원" 뱃지)
- **3. 지역과 소속**: 활동 지역("예: 서울 송파구") · 소속팀 검색(선택, "없으면 비워두세요") + 안내 박스(오렌지 점선 `#fff6f1`): ✉️ 초대코드 안내. **"이 단계 건너뛰기" 링크 제공.**
버튼: "← 이전"(스텝2+) / "다음 →" / 마지막 "프로필 만들기" → 완료(player)

## ③b 팀 온보딩 (`/onboarding/team`) — 폭 560px, 2스텝
- **1. 팀 정보**: 엠블럼 업로드(76px 점선 박스) + 팀 이름("예: FC 한강 유나이티드") · 팀 유형 칩 클럽/학교/학원 · 활동 지역 + 안내 박스: 🏠 "다음 단계를 마치면 **공개 팀 홈페이지**(/team/한강유나이티드)가 자동으로 만들어져요." 버튼 "다음: 선수단 등록 →"
- **2. 선수단(로스터) 등록**: 입력 행 [선수 이름 | 포지션 | 등번호(72px) | ＋추가(네이비)] → 아래 리스트에 행 추가.
  행 구성: 등번호 원(34px, `#f1f3f8` 배경, 네이비 숫자) · 이름(700) · 포지션 · **"Unclaimed" 뱃지**(회색 pill) · ✕ 삭제(hover 시 붉은톤). 리스트 max-height 260px 스크롤.
  안내 박스(오렌지 점선): 💡 "등록한 선수마다 **선수 프로필이 자동 생성**되고… **초대코드**가 만들어져요…"
  버튼 "팀 홈페이지 만들기 🎉" / 링크 "나중에 등록할게요"
검증: 이름 비어 있으면 추가 무시. 초기 시드 2행(김민준 FW 9 / 이서준 MF 8)은 데모용 — 실서비스는 빈 리스트 시작.

## ④ 완료 — 카드 480px, 중앙 정렬
아이콘 원(72px, teal 12% 배경 + teal 40% 보더) + 타이틀(24px/800) + 본문. 분기 3종:
| 경로 | 아이콘 | 타이틀 | 부가 요소 |
|---|---|---|---|
| 선수 | ⚽ | "선수 프로필이 만들어졌어요!" | — |
| 팀 | 🏛️ | "팀 홈페이지가 만들어졌어요!" | URL 칩(`playground.kr/team/{slug}` + "공개됨" teal 뱃지) + "등록 선수 n명 · 초대코드 n개 발급됨" |
| General(스킵) | 👋 | "환영해요!" | — |
버튼 "대시보드로 가기" (오렌지).

---

## 완료 기준 체크리스트
- [ ] 플로우: 로그인→역할선택→온보딩→완료 전이가 명세와 일치, 스킵 경로 2개(역할 스킵/선수 3스텝 스킵) 동작
- [ ] General 자동 가입 + 역할 미선택 상태 처리 (Records 진입 시 유도 배너는 별도 티켓)
- [ ] 에이전트 카드 비활성 (클릭 무효, feature flag로 추후 활성화 가능한 구조)
- [ ] 로스터 추가/삭제 즉시 반영, 빈 이름 방지, 완료 화면에 선수 수 정확히 표시
- [ ] 소셜 버튼 = 공식 브랜드 에셋 (색상·비율 각 사 가이드 준수)
- [ ] 입력 포커스 아웃라인 표시, 모든 터치 타겟 ≥44px
- [ ] 카피 원문 유지, 오렌지는 주 CTA에만
