{
  "//": [
    "이 파일은 WASM으로 브라우저에 공개 서빙된다 — 누구나 볼 수 있으므로 시크릿 절대 금지.",
    "공개 가능한 값만 둔다: 피처 플래그, 공개 API 주소, 공개 분석 키 등.",
    "환경별 값은 appsettings.{Development,Staging,Production}.json — 호스팅 Server가 전파한 환경(Blazor-Environment)에 맞춰 자동 로드된다.",
    "시크릿이 필요한 처리는 Client가 아니라 인증된 Server API 뒤에 둔다 (Client는 토큰으로 호출만)."
  ],
  "Features": {
    "AgentEnabled": false
  }
}
