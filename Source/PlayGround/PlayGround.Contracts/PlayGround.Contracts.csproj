<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>PlayGround.Contracts</RootNamespace>
    <AssemblyName>PlayGround.Contracts</AssemblyName>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- Core.Shared = 시각 타입(SystemTime), Domain = 도메인 어휘(enum). DTO는 여전히 순수 데이터만 담는다 -->
    <ProjectReference Include="..\..\Core\Shared\Core.Shared.csproj" />
    <ProjectReference Include="..\PlayGround.Domain\PlayGround.Domain.csproj" />
  </ItemGroup>

</Project>
